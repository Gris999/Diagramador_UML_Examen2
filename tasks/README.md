# Tasks

Each significant implementation task should have a small Markdown file here.

Suggested format:

- objective;
- owner/agent;
- branch;
- files/components likely affected;
- acceptance criteria;
- test command;
- status.

Only one writer should own a feature branch at a time.
